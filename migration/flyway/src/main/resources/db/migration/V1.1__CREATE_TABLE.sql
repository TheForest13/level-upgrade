CREATE TABLE COMPANY.COMPANY(
                    	ID INTEGER NOT NULL,
                    	TITLE VARCHAR(255),
                    	DESCRIPTION VARCHAR(255),
                    	PRIMARY KEY (ID)
                    );